<div class="nav">
	<!-- Loop through the navigation items -->
	{{#foreach navigation}}
	<span class="nav-{{slug}}{{#if current}} nav-current{{/if}}">
		<a href="{{url absolute="true"}}">{{label}}</a> | 
	</span>
	{{/foreach}}
	<!-- End the loop -->
</div>